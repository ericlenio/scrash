-clear-cache
#ssh ra bash -c -- '-download /etc/passwd'
if [ "$SCR_SSH_AUTH_SOCK" ]; then
  hostname=$(ssh localhost hostname)
  [ "$hostname" ]
  [ "$hostname" = $(hostname) ]
else
  echo 'WARNING: skipping tests, no value for $SCR_SSH_AUTH_SOCK' >&2
fi
